version: 2
jobs:
  test:
    docker:
      - image: node:8
    steps:
      - checkout
      - run: yarn install
      - run: yarn test
  build:
    docker: 
      - image: node:8
    steps:
      - checkout
      - run: yarn install
      - run: yarn build
      - store_artifacts:
          path: ~/project/build
      - persist_to_workspace:
          root: ~/project
          paths:
            - build/*
  deploy:
    docker:
      - image: node:8
    steps:
      - attach_workspace:
          at: ~/project
      - run: "npm install surge --global"
      - run: "surge --project ./build --domain $url"
workflows:
  version: 2
  test-build-deploy:
    jobs:
      - test
      - build
      - deploy:
          requires:
            - test
            - build
          filters:
            branches:
              only: master
