image: node:8

stages:
  - test
  - build

before_script:
  - yarn install

cache:
  paths:
    - node_modules/

test:
  stage: test
  script:
    - yarn test

build:
  stage: build
  script:
    - yarn build
  artifacts:
    paths:
      - build/
